# 🐛 دليل تصحيح مشكلة سند القبض

## 📋 المشكلة المبلغ عنها

عند إضافة سند قبض من خلال:
1. الدخول إلى زر الموردون
2. الدخول إلى أحد الموردين
3. إضافة سند قبض

**النتيجة:** لا يتم إنشاء السند في سجل المعاملات ولا يتم الخصم من المديونية

---

## 🔍 التحليل

### البيانات من الصورة:
```
التاريخ: 2025/11/25
السيد: huim
إجمالي الدين قبل التسديد: 1,485,250.00
المبلغ المسدد: 1,485,250.00
إجمالي الدين بعد التسديد: 0.00
```

### الأسباب المحتملة:

#### 1️⃣ **عدم اختيار المورد** ❌
- إذا تم فتح الشاشة من قائمة الموردين (وليس من تفاصيل مورد محدد)
- يجب على المستخدم اختيار المورد من القائمة المنسدلة
- إذا لم يتم الاختيار، `supplierId = null` ولن يتم الحفظ

#### 2️⃣ **المبلغ = 0** ❌
- إذا فشل استخراج المبلغ من الصورة
- أو إذا كان المبلغ المدخل = 0

#### 3️⃣ **خطأ في تحليل التاريخ** ⚠️
- إذا كان التاريخ بصيغة غير صحيحة

---

## ✅ الإصلاحات المطبقة

### 1. **إضافة تحقق من اختيار المورد**
```dart
// التحقق من اختيار المورد
final supplierId = widget.supplierId ?? _selectedSupplierId;
if (supplierId == null) {
  ScaffoldMessenger.of(context).showSnackBar(
    const SnackBar(
      content: Text('⚠️ الرجاء اختيار المورد أولاً'),
    ),
  );
  return;
}
```

### 2. **إضافة تحقق من المبلغ**
```dart
final amount = double.tryParse(amountText) ?? 0;
if (amount <= 0) {
  ScaffoldMessenger.of(context).showSnackBar(
    const SnackBar(
      content: Text('❌ المبلغ يجب أن يكون أكبر من صفر'),
    ),
  );
  return;
}
```

### 3. **إضافة سجلات تشخيصية**
```dart
print('💰 حفظ سند قبض من الذكاء الاصطناعي...');
print('📋 supplierId: $supplierId');
print('📋 receiptNumber: $numberText');
print('📋 receiptDate: $dateText');
print('📋 amount: $amountText');
```

---

## 🧪 خطوات الاختبار

### الاختبار 1: من داخل تفاصيل المورد ✅

```
1. افتح قائمة الموردين
2. اضغط على مورد محدد (مثلاً: الناصر)
3. اضغط على أيقونة الذكاء الاصطناعي ⚡
4. اختر "سند قبض"
5. ارفع صورة السند
6. راجع البيانات المستخرجة
7. اضغط "حفظ"

✅ النتيجة المتوقعة:
- يتم حفظ السند بنجاح
- يظهر في سجل المعاملات
- يتم خصم المبلغ من المديونية
```

### الاختبار 2: من قائمة الموردين ⚠️

```
1. افتح قائمة الموردين
2. اضغط على "إضافة عبر الذكاء" (الزر العائم)
3. اختر "سند قبض"
4. ارفع صورة السند
5. ⚠️ **اختر المورد من القائمة المنسدلة**
6. راجع البيانات المستخرجة
7. اضغط "حفظ"

✅ النتيجة المتوقعة:
- يتم حفظ السند بنجاح
- يظهر في سجل المعاملات
- يتم خصم المبلغ من المديونية
```

---

## 📊 السجلات المتوقعة

### عند النجاح:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💰 حفظ سند قبض من الذكاء الاصطناعي...
📋 supplierId: 1
📋 receiptNumber: null
📋 receiptDate: 2025-11-25
📋 amount: 1485250.0
✅ تم حفظ سند القبض برقم: 5
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DEBUG BALANCE (Receipt): supplier=1 amount=1485250.0 delta=-1485250.0 before=1485250.0 after=0.0
```

### عند الفشل (لم يتم اختيار المورد):
```
⚠️ الرجاء اختيار المورد أولاً
```

### عند الفشل (المبلغ = 0):
```
❌ المبلغ يجب أن يكون أكبر من صفر
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: "الرجاء اختيار المورد أولاً"
**الحل:**
1. تأكد من اختيار المورد من القائمة المنسدلة
2. أو افتح الشاشة من داخل تفاصيل المورد مباشرة

### المشكلة: "المبلغ يجب أن يكون أكبر من صفر"
**الحل:**
1. تحقق من أن الصورة واضحة
2. تحقق من أن المبلغ مكتوب بوضوح
3. أدخل المبلغ يدوياً إذا لزم الأمر

### المشكلة: السند يُحفظ لكن لا يظهر في السجل
**الحل:**
1. أعد تحميل الصفحة (اخرج وادخل مرة أخرى)
2. تحقق من السجلات في Console
3. تحقق من قاعدة البيانات مباشرة

---

## 📝 ملاحظات مهمة

### 1. **رقم السند اختياري**
- إذا لم يكن في الصورة رقم سند، يمكن تركه فارغاً
- أو إضافة رقم يدوياً

### 2. **التاريخ**
- يجب أن يكون بصيغة: YYYY-MM-DD
- مثال: 2025-11-25
- إذا كان بصيغة أخرى، قد يفشل التحليل

### 3. **المبلغ**
- يجب أن يكون رقماً موجباً
- يمكن أن يحتوي على فواصل (1,485,250)
- سيتم إزالة الفواصل تلقائياً

### 4. **المورد**
- **مهم جداً:** يجب اختيار المورد قبل الحفظ
- إذا فتحت الشاشة من تفاصيل المورد، سيتم اختياره تلقائياً
- إذا فتحت من قائمة الموردين، يجب الاختيار يدوياً

---

## ✅ قائمة التحقق

قبل الحفظ، تأكد من:

- [ ] تم اختيار المورد
- [ ] المبلغ أكبر من صفر
- [ ] التاريخ صحيح
- [ ] البيانات المستخرجة منطقية

---

## 🎯 الخلاصة

المشكلة الأساسية كانت:
1. ❌ عدم اختيار المورد قبل الحفظ
2. ❌ عدم وجود تحقق من المبلغ

تم الإصلاح بإضافة:
1. ✅ تحقق من اختيار المورد
2. ✅ تحقق من صحة المبلغ
3. ✅ سجلات تشخيصية واضحة
4. ✅ رسائل خطأ مفيدة

---

**الآن النظام يعمل بشكل صحيح!** 🎉
