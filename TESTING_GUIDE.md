# 🧪 دليل اختبار نظام الذكاء الاصطناعي

## 📋 خطوات الاختبار

### 1️⃣ **اختبار Groq API (الأولوية الأولى)**

#### السيناريو: Groq ينجح
```
1. تأكد من وجود GROQ_API_KEY في .env
2. افتح شاشة الموردين
3. اضغط على "إضافة عبر الذكاء"
4. اختر "فاتورة شراء"
5. ارفع صورة فاتورة واضحة
6. راقب Console للسجلات

✅ النتيجة المتوقعة:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🤖 بدء استخراج البيانات من الصورة
📄 نوع الملف: image/jpeg
📋 نوع الاستخراج: invoice
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔵 المحاولة 1: استخدام Groq API...
✅ نجح Groq! تم استخراج البيانات بنجاح
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### 2️⃣ **اختبار Fallback إلى Gemini**

#### السيناريو: Groq يفشل، Gemini ينجح
```
1. ضع GROQ_API_KEY خاطئ مؤقتاً
2. تأكد من صحة GEMINI_API_KEY
3. كرر خطوات الاختبار السابقة

✅ النتيجة المتوقعة:
🔵 المحاولة 1: استخدام Groq API...
❌ خطأ HTTP من Groq: 401
   السبب: مشكلة في المصادقة
🔄 الانتقال إلى Gemini...

🟢 المحاولة 2: استخدام Gemini API...
✅ نجح Gemini! تم استخراج البيانات بنجاح
```

---

### 3️⃣ **اختبار فشل كلا الـ APIs**

#### السيناريو: كلاهما يفشل
```
1. ضع كلا المفتاحين خاطئين
2. حاول رفع صورة

✅ النتيجة المتوقعة:
❌ فشلت جميع محاولات الاستخراج
رسالة خطأ للمستخدم
```

---

### 4️⃣ **اختبار أنواع الملفات**

#### الملفات المدعومة:
- ✅ PNG
- ✅ JPG/JPEG
- ✅ PDF

#### اختبار كل نوع:
```
1. ارفع صورة PNG → يجب أن ينجح
2. ارفع صورة JPG → يجب أن ينجح
3. ارفع ملف PDF → يجب أن ينجح
```

---

### 5️⃣ **اختبار سندات القبض**

```
1. اضغط على "إضافة عبر الذكاء"
2. اختر "سند قبض"
3. ارفع صورة سند قبض

✅ النتيجة المتوقعة:
استخراج البيانات بنجاح:
- receipt_date
- receipt_number
- amount
- payment_method
```

---

## 🔍 نقاط التحقق

### ✅ قائمة التحقق الكاملة:

- [ ] Groq API يعمل بشكل صحيح
- [ ] Gemini API يعمل كـ Fallback
- [ ] رسائل الخطأ واضحة
- [ ] السجلات تظهر في Console
- [ ] جميع أنواع الملفات مدعومة
- [ ] استخراج الفواتير يعمل
- [ ] استخراج سندات القبض يعمل
- [ ] البيانات المستخرجة صحيحة
- [ ] حفظ البيانات في قاعدة البيانات يعمل
- [ ] تحديث أسعار المنتجات يعمل

---

## 🐛 استكشاف الأخطاء

### المشكلة: "لم يتم العثور على أي API Key"
**الحل:**
1. تحقق من ملف `.env`
2. تأكد من وجود `GROQ_API_KEY` أو `GEMINI_API_KEY`
3. أعد تشغيل التطبيق

### المشكلة: "Groq error: 401"
**الحل:**
1. تحقق من صحة `GROQ_API_KEY`
2. تأكد من عدم انتهاء صلاحية المفتاح
3. النظام سينتقل تلقائياً إلى Gemini

### المشكلة: "Groq error: 429"
**الحل:**
1. تجاوزت الحصة المجانية
2. انتظر قليلاً أو استخدم Gemini
3. النظام سينتقل تلقائياً إلى Gemini

### المشكلة: "Groq error: 400"
**الحل:**
1. قد يكون نوع الملف غير مدعوم
2. حاول تحويل الملف إلى JPG
3. النظام سينتقل تلقائياً إلى Gemini

---

## 📊 مقارنة الأداء

### Groq API:
- ⚡ **السرعة:** سريع جداً (2-5 ثواني)
- 💰 **التكلفة:** مجاني (حصة محدودة)
- 🎯 **الدقة:** عالية جداً
- 📁 **الملفات:** PNG, JPG, PDF

### Gemini API:
- ⚡ **السرعة:** متوسط (5-10 ثواني)
- 💰 **التكلفة:** مجاني (حصة أكبر)
- 🎯 **الدقة:** عالية
- 📁 **الملفات:** PNG, JPG, PDF

---

## 🎯 نصائح للحصول على أفضل النتائج

1. **جودة الصورة:**
   - استخدم صور واضحة وعالية الدقة
   - تأكد من الإضاءة الجيدة
   - تجنب الظلال والانعكاسات

2. **نوع الملف:**
   - JPG أفضل للصور الفوتوغرافية
   - PNG أفضل للصور الممسوحة ضوئياً
   - PDF للمستندات الرسمية

3. **محتوى الفاتورة:**
   - تأكد من وضوح الأرقام
   - تأكد من وضوح أسماء المنتجات
   - تأكد من وجود التاريخ والرقم

---

## 📝 سجل الاختبار

### التاريخ: _________

| الاختبار | النتيجة | ملاحظات |
|---------|---------|---------|
| Groq API | ⬜ نجح ⬜ فشل | |
| Gemini Fallback | ⬜ نجح ⬜ فشل | |
| PNG | ⬜ نجح ⬜ فشل | |
| JPG | ⬜ نجح ⬜ فشل | |
| PDF | ⬜ نجح ⬜ فشل | |
| فاتورة | ⬜ نجح ⬜ فشل | |
| سند قبض | ⬜ نجح ⬜ فشل | |

---

**ملاحظة:** احتفظ بهذا الدليل للرجوع إليه عند الحاجة!
