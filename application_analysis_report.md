# تقرير تحليل التطبيق الشامل
**تاريخ التقرير:** 2024-12-10
**الهدف:** تقديم تحليل مفصل وشامل لكافة ميزات ووظائف التطبيق وأكواد البرمجة الخاصة به، لفهم آلية عمله دون إجراء تعديلات.

---

## 1. نظرة عامة على التطبيق
التطبيق عبارة عن نظام متكامل لإدارة المبيعات والديون ("دفتة ديون")، مصمم خصيصاً لتلبية احتياجات الأعمال التجارية التي تتعامل مع بيع المنتجات بوحدات متعددة (قطع، أمتار، كراتين) وتدير حسابات عملاء معقدة (ديون، تسديدات). يتميز التطبيق بواجهة عربية بالكامل، دعم قوي للمزامنة السحابية عبر Google Drive، ونظام تقارير وتحليلات مالي متقدم.

---

## 2. نماذج البيانات (Data Models)

### أ. نموذج العميل (`Customer`)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | `int?` | المعرف الفريد |
| `name` | `String` | اسم العميل |
| `phone` | `String?` | رقم الهاتف |
| `address` | `String?` | العنوان |
| `currentTotalDebt` | `double` | إجمالي الدين الحالي |
| `audioNotePath` | `String?` | مسار الملاحظة الصوتية |

### ب. نموذج المعاملة (`DebtTransaction`)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | `int?` | المعرف الفريد |
| `customerId` | `int` | معرف العميل |
| `transactionDate` | `DateTime` | تاريخ المعاملة |
| `amountChanged` | `double` | المبلغ (موجب للدين، سالب للتسديد) |
| `balanceBeforeTransaction` | `double?` | الرصيد قبل المعاملة |
| `newBalanceAfterTransaction` | `double?` | الرصيد بعد المعاملة |
| `transactionType` | `String` | النوع: `manual_debt`, `manual_payment`, `invoice`, الخ |
| `transactionUuid` | `String?` | معرف المزامنة الفريد (UUID) |
| `invoiceId` | `int?` | معرف الفاتورة المرتبطة (إن وجد) |

### ج. نموذج الفاتورة (`Invoice`)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | `int?` | المعرف الفريد |
| `customerName` | `String` | اسم العميل |
| `installerName` | `String?` | اسم المؤسس/الفني |
| `invoiceDate` | `DateTime` | تاريخ الفاتورة |
| `paymentType` | `String` | نوع الدفع: `نقد` أو `آجل` |
| `totalAmount` | `double` | إجمالي الفاتورة |
| `discount` | `double` | الخصم |
| `amountPaidOnInvoice` | `double` | المبلغ المدفوع |
| `loadingFee` | `double` | أجور التحميل |
| `status` | `String` | الحالة: `محفوظة` أو `معلقة` |
| `returnAmount` | `double` | مبلغ المرتجع |
| `isLocked` | `bool` | هل الفاتورة مقفلة؟ |
| `pointsRate` | `double` | معدل النقاط (نقاط لكل 100,000) |

### د. نموذج المنتج (`Product`)
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `id` | `int?` | المعرف الفريد |
| `name` | `String` | اسم المنتج |
| `unitOfSale` | `String` | وحدة البيع: `قطعة` أو `متر` |
| `price1` - `price5` | `double` | مستويات الأسعار المختلفة |
| `costPrice` | `double` | سعر التكلفة |
| `unitHierarchy` | `String?` | النظام الهرمي للوحدات (JSON) |
| `unitCosts` | `String?` | تكلفة كل مستوى وحدة (JSON) |

---

## 3. تحليل الشاشات والوظائف التفصيلية

### أ. الشاشة الرئيسية (`HomeScreen`)
تعتبر مركز التحكم في التطبيق.
*   **الوظائف والأزرار:**
    *   **قائمة العملاء:** تعرض العملاء مع رصيد كل منهم. تدعم البحث السريع والفرز حسب الاسم أو آخر معاملة.
    *   **زر المزامنة (أعلى اليسار):** يفتح حوار المزامنة (Dialog) الذي يديره `_showSyncDialog` و `SyncService`. يقوم برفع وتنزيل البيانات من Google Drive مع عرض حالة التقدم.
    *   **القائمة الجانبية (Drawer):** تحتوي على روابط سريعة:
        *   تسجيل الدخول لـ Google Drive.
        *   رفع نسخة احتياطية (يدوي).
        *   الدردشة الذكية (AI Chat).
        *   الاقتراحات والشكاوى.
    *   **الأزرار العائمة (Quick Actions):**
        *   **إضافة عميل:** لإنشاء ملف عميل جديد.
        *   **سجل الديون:** لعرض سجل شامل للعمليات.
        *   **إدخال بضاعة:** لإضافة منتجات جديدة للمخزن.
        *   **إنشاء قائمة (فاتورة):** الانتقال لشاشة البيع.

### ب. شاشة إضافة معاملة (`AddTransactionScreen`)
هذه الشاشة تتيح إضافة دين أو تسجيل تسديد يدوياً للعميل.
*   **الميزات:**
    *   **اختيار نوع المعاملة:** `SegmentedButton` للتبديل بين "إضافة دين" و "تسديد دين".
    *   **حقل المبلغ:** مع تنسيق تلقائي للآلاف (`ThousandSeparatorInputFormatter`).
    *   **معاينة فورية:** يعرض الرصيد الحالي والرصيد المتوقع بعد الحفظ.
    *   **تسجيل صوتي:** زر لتسجيل ملاحظة صوتية مرتبطة بالمعاملة.
    *   **سند القبض:** عند التسديد، يُسأل المستخدم إذا أراد طباعة سند قبض PDF.
*   **الأمان المالي (`MoneyCalculator`):**
    *   يستخدم التطبيق أداة `MoneyCalculator` للتحقق المزدوج من صحة كل عملية حسابية.
    *   يتم استخدام `MoneyCalculator.verifyTransaction()` للتأكد من أن `الرصيد قبل + المبلغ = الرصيد بعد`.
    *   يمنع التطبيق التسديد الذي يجعل الرصيد سالباً (أي دفع أكثر من المستحق).

### ج. شاشة إنشاء الفاتورة (`CreateInvoiceScreen`)
هذه أكثر الشاشات تعقيداً وأهمية في التطبيق.
*   **الميزات الأساسية:**
    *   **اختيار العميل:** قائمة منسدلة أو بحث لاختيار العميل، مع إمكانية إنشاء عميل جديد مباشرة.
    *   **إضافة المنتجات:** يمكن البحث عن المنتج بالاسم أو الباركود أو المعرف.
    *   **أنظمة الوحدات:** يدعم التطبيق بذكاء الوحدات المختلفة (قطعة، متر، كرتون). عند اختيار "كرتون" مثلاً، يقوم التطبيق تلقائياً بحساب السعر بناءً على عدد القطع داخل الكرتون.
    *   **التسعير المتعدد:** يمكن للمستخدم الاختيار بين مستويات أسعار مختلفة (سعر 1، سعر 2، الخ) لكل منتج.
    *   **المؤسس/الفني:** حقل لربط الفاتورة بمؤسس معين لاحتساب نقاطه.
    *   **الحسابات المالية (الربح):** زر محمي بكلمة مرور لعرض ربح الفاتورة فورياً.
    *   **خيارات الدفع:**
        *   **نقد:** دفع كامل المبلغ.
        *   **آجل (دين):** تسجيل المبلغ على ذمة العميل.
    *   **الحفظ التلقائي:** يتم حفظ حالة الفاتورة (Draft) تلقائياً باستخدام `FlutterSecureStorage`.
    *   **التسوية (Settlement):** إمكانية تعديل الفواتير القديمة، حيث يقوم النظام بحساب الفروقات المالية وعكس القيود المحاسبية اللازمة تلقائياً.

### د. شاشة تفاصيل العميل (`CustomerDetailsScreen`)
ملف شامل لكل عميل.
*   **الوظائف:**
    *   **معلومات الاتصال:** زر اتصال سريع، زر واتساب (يفتح المحادثة مباشرة)، زر الموقع الجغرافي.
    *   **الملاحظات الصوتية:** ميزة تسجيل أو تشغيل ملاحظات صوتية خاصة بالعميل.
    *   **سجل العمليات:** قائمة بجميع الفواتير، الدفعات، والديون مع إمكانية الفلترة.
    *   **الإجراءات المالية:**
        *   **إضافة دفعة/دين:** يفتح `AddTransactionScreen`.
        *   **كشف حساب PDF:** إنشاء ملف PDF بتفاصيل الحساب ومشاركته.
        *   **كشف حساب تجاري:** تقرير مالي دقيق جداً (مثل كشوفات البنوك) يوضح الرصيد قبل وبعد كل حركة.
        *   **أرشيف سندات القبض:** عرض جميع سندات القبض المطبوعة للعميل.
        *   **فحص السلامة المالية:** أداة برمجية تقوم بإعادة حساب جميع عمليات العميل للتأكد من تطابق الرصيد.

### هـ. شاشة الديون المتأخرة (`OverdueDebtsScreen`)
*   **الغرض:** عرض قائمة العملاء الذين لم يسددوا منذ فترة زمنية محددة.
*   **الميزات:**
    *   **تصفية الفترة:** يمكن اختيار الفترة (7، 14، 30، 60، 90 يوماً).
    *   **التلوين حسب الخطورة:** الديون المتأخرة جداً (>90 يوم) تظهر باللون الأحمر، والمتوسطة بالبرتقالي.
    *   **ملخص إحصائي:** يعرض عدد العملاء وإجمالي الديون المتأخرة.
    *   **الانتقال لتفاصيل العميل:** بالنقر على أي عميل يتم فتح صفحة تفاصيله.

### و. إدارة المخزن والمنتجات
1.  **إدخال بضاعة (`ProductEntryScreen`):**
    *   نظام هرمي للوحدات: تعريف الوحدة الصغرى (قطعة) والوحدة الكبرى (كرتون = 12 قطعة مثلاً).
    *   تحديد سعر التكلفة وسعر البيع لكل مستوى.
    *   للمنتجات التي تباع بالمتر، يمكن تحديد "طول اللفة".
2.  **تعديل المنتجات (`EditProductsScreen`):**
    *   شاشة محمية بكلمة مرور.
    *   تتيح تعديل الأسعار، الكميات، أو تصحيح بيانات منتج موجود.
3.  **الجرد والتحليلات (`InventoryScreen`):**
    *   **الجرد الشهري:** يعرض ملخص المبيعات (نقد/آجل)، الأرباح، والتكاليف لكل شهر.
    *   **المقارنة:** تبويب يقارن أداء الشهر الحالي بالشهر السابق (نسب النمو%).
    *   **أفضل العملاء:** ترتيب العملاء حسب إجمالي المشتريات أو الربحية.
    *   **أفضل المنتجات:** ترتيب المنتجات حسب المبيعات أو الأرباح.

### ز. قسم المؤسسين/الفنيين (`InstallersListScreen`)
نظام خاص لإدارة الفنيين أو "المؤسسين" الذين يتعاملون مع المحل.
*   **نظام النقاط:** كل عملية شراء يقوم بها الفني تحسب له نقاطاً (معدل قابل للتخصيص، افتراضياً نقطة لكل 100,000 دينار).
*   **الفوترة:** تتبع إجمالي المبيعات المحسوبة لكل فني.
*   **إدارة النقاط:** إمكانية إضافة أو خصم نقاط يدوياً، مع تحديد السبب وتسجيله في السجل.

### ح. التقارير (`ReportsScreen`)
مركز التقارير الشامل، يتضمن 7 أنواع:
| التقرير | الأيقونة | الوصف |
|---------|---------|-------|
| تقارير البضاعة | 📦 | حركة صنف معين، الأكثر مبيعاً |
| تقارير الأشخاص | 👥 | أرصدة العملاء، تقارير الأرباح لكل عميل |
| تقرير اليوم | 📅 | مبيعات وأرباح اليوم الحالي |
| تقرير الأسبوع | 📆 | ملخص أسبوعي |
| تقرير الشهر | 📈 | تقرير شهري مفصل |
| تقرير السنة | 📊 | ملخص سنوي شامل |
| الديون المتأخرة | ⚠️ | العملاء المتأخرين عن السداد |

### ط. الإعدادات العامة (`GeneralSettingsScreen`)
*   **تخصيص المظهر:** تغيير ألوان التطبيق (الثيم)، ألوان الأزرار، وحقول النصوص.
*   **معلومات الشركة:** إدخال اسم الشركة، الوصف، وأرقام الهواتف (تظهر على الفواتير المطبوعة).
*   **إعدادات الفاتورة:** التحكم في التمرير التلقائي (Auto-scroll).
*   **إعدادات المؤسسين:** تحديد قيمة المبيعات مقابل كل نقطة.
*   **أدوات المطور/الأمان:**
    *   فحص نزاهة شامل لقاعدة البيانات.
    *   إعادة حساب كل الديون.
    *   ملخص مالي شامل.
    *   مشاركة جميع كشوفات الحسابات.

---

## 4. الخدمات الأساسية والبنية التقنية

### أ. قاعدة البيانات (`DatabaseService`)
خدمة شاملة تحتوي على أكثر من **200 دالة** تغطي جميع العمليات.

**أهم الدوال:**
*   `insertCustomer()`, `updateCustomer()`, `deleteCustomer()` - إدارة العملاء
*   `insertTransaction()`, `updateManualTransaction()` - إدارة المعاملات
*   `saveCompleteInvoice()` - حفظ الفاتورة مع جميع عناصرها
*   `verifyCustomerBalance()` - التحقق من صحة رصيد العميل
*   `recalculateAndApplyCustomerDebt()` - إعادة حساب الدين من جميع المعاملات
*   `recalculateCustomerTransactionBalances()` - إعادة حساب الرصيد بعد كل معاملة
*   `checkAndRepairDatabaseIntegrity()` - فحص وإصلاح قاعدة البيانات

**آليات الأمان:**
*   **القفل المتزامن (Locking):** `_acquireCustomerLock()` و `_releaseCustomerLock()` لمنع التعديل المتزامن على نفس العميل.
*   **المعاملات الذرية (Atomic Transactions):** جميع العمليات المالية تتم داخل `db.transaction()` لضمان عدم التناقض.
*   **تتبع المزامنة:** كل عملية تحصل على `syncUuid` فريد لتسهيل المزامنة.

### ب. خدمة المزامنة (`SyncService`)
*   **حالات المزامنة (`SyncStatus`):**
    *   `idle` - في انتظار
    *   `connecting` - جاري الاتصال
    *   `syncing` - جاري المزامنة
    *   `success` - نجحت
    *   `failed` - فشلت
    *   `offline` - لا يوجد اتصال
    *   `notSignedIn` - غير مسجل الدخول
*   **نتيجة المزامنة (`SyncResult`):** تحتوي على:
    *   `downloaded` - عدد العمليات التي تم تنزيلها
    *   `uploaded` - عدد العمليات التي تم رفعها
    *   `applied` - عدد العمليات التي تم تطبيقها
    *   `duration` - مدة المزامنة
*   **الإعدادات (`OptimizedSyncConfig`):**
    *   `maxStorageMB: 300` - الحد الأقصى للتخزين
    *   `maxSnapshotsToKeep: 3` - عدد اللقطات المحفوظة
    *   `enableCompression: true` - تفعيل الضغط
    *   `snapshotEveryNOperations: 200` - لقطة كل 200 عملية

### ج. سند القبض (`ReceiptVoucherPdfService`)
*   **الغرض:** إنشاء سند قبض PDF عند تسديد العميل.
*   **المحتويات:**
    *   رقم السند (تسلسلي تلقائي)
    *   اسم العميل
    *   الدين قبل التسديد
    *   المبلغ المسدد (بلون أخضر)
    *   الدين بعد التسديد
    *   التاريخ والوقت
    *   تنبيه حفظ الورقة
*   **التصميم:** يستخدم نفس رأس الصفحة الخاص بالفواتير (شعار الشركة).

### د. أداة الحساب الآمن (`MoneyCalculator`)
*   يتم استخدام هذه الأداة في جميع الحسابات المالية لمنع أخطاء الفاصلة العائمة (Floating Point Errors).
*   **الدوال:**
    *   `add(a, b)` - جمع
    *   `subtract(a, b)` - طرح
    *   `multiply(a, b)` - ضرب
    *   `divide(a, b)` - قسمة
    *   `verifyTransaction()` - التحقق من صحة المعاملة

---

## 5. جداول قاعدة البيانات الرئيسية

| الجدول | الوصف |
|--------|-------|
| `customers` | بيانات العملاء وأرصدتهم |
| `products` | بيانات المنتجات والأسعار |
| `debt_transactions` | سجل المعاملات (ديون، تسديدات، فواتير) |
| `invoices` | رؤوس الفواتير |
| `invoice_items` | تفاصيل بنود الفواتير |
| `invoice_adjustments` | تسويات الفواتير |
| `installers` | بيانات المؤسسين |
| `installer_points` | سجل نقاط المؤسسين |
| `customer_receipt_vouchers` | سندات القبض |
| `sync_operations` | عمليات المزامنة المعلقة |

---

## 6. ملاحظات التصميم وتجربة المستخدم (UI/UX)

*   **التوجيه:** التطبيق مبني بنمط (Right-to-Left) كامل لدعم اللغة العربية.
*   **تنسيق الأرقام:** يتم عرض جميع المبالغ المالية بتنسيق سهل القراءة مع فواصل الآلاف (مثلاً `15,000`).
*   **التغذية الراجعة:** يعتمد التطبيق على الـ `SnackBars` و الـ `Dialogs` لإبلاغ المستخدم بنجاح أو فشل العمليات.
*   **الألوان:**

| العنصر | اللون | الكود |
|--------|-------|-------|
| الأساسي | أزرق نيلي | `#3F51B5` |
| النجاح/الربح | أخضر | `#4CAF50` |
| الخطأ/الدين | أحمر | `#F44336` |
| التحذير | برتقالي | `#FF9800` |
| التقارير | بنفسجي | `#6C63FF` |

---

## 7. الاعتماديات الخارجية (Dependencies)

| المكتبة | الاستخدام |
|---------|----------|
| `sqflite` | قاعدة البيانات المحلية |
| `provider` | إدارة الحالة |
| `pdf` & `printing` | إنشاء وطباعة PDF |
| `googleapis` | الاتصال بـ Google Drive |
| `google_sign_in` | تسجيل الدخول بـ Google |
| `share_plus` | مشاركة الملفات |
| `audioplayers` & `flutter_sound` | تسجيل وتشغيل الصوت |
| `url_launcher` | فتح الروابط (واتساب، اتصال) |
| `flutter_secure_storage` | تخزين آمن للمسودات |
| `intl` | تنسيق الأرقام والتواريخ |
| `record` | تسجيل الملاحظات الصوتية |

---

## 8. شاشة تفاصيل الفاتورة (`InvoiceDetailsScreen`)

شاشة عرض تفصيلي للفاتورة مع حسابات الربح لكل بند.

### المحتويات:
1.  **رأس الفاتورة:**
    *   رقم الفاتورة والحالة (محفوظة/معلقة)
    *   اسم العميل، الهاتف، العنوان
    *   التاريخ ونوع الدفع
    *   اسم المؤسس (إن وجد)

2.  **قائمة المنتجات:**
    *   لكل منتج يتم عرض:
        *   اسم المنتج وإجمالي السعر
        *   الكمية والوحدة والسعر المطبق
        *   **تفاصيل الربح:** (محاطة بإطار ملون)
            *   سعر التكلفة
            *   الربح لكل وحدة
            *   الربح الإجمالي (أخضر للربح، أحمر للخسارة)
            *   نسبة الربح المئوية

3.  **ملخص الفاتورة:**
    *   المجموع الكلي
    *   الخصم (إن وجد)
    *   المرتجع (إن وجد)
    *   المبلغ المدفوع
    *   المبلغ المتبقي (باللون الأحمر)

---

## 9. نموذج بند الفاتورة (`InvoiceItem`)

نموذج متقدم يحتوي على كل تفاصيل بند الفاتورة.

### الحقول الرئيسية:
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `productId` | `int?` | معرف المنتج |
| `productName` | `String` | اسم المنتج |
| `unit` | `String` | الوحدة (قطعة/متر/كرتون) |
| `unitPrice` | `double` | سعر البيع للوحدة |
| `costPrice` | `double?` | سعر التكلفة |
| `actualCostPrice` | `double?` | التكلفة الفعلية وقت البيع |
| `quantityIndividual` | `double?` | الكمية بالوحدات الصغيرة |
| `quantityLargeUnit` | `double?` | الكمية بالوحدات الكبيرة |
| `appliedPrice` | `double` | السعر المطبق فعلياً |
| `itemTotal` | `double` | إجمالي البند |
| `saleType` | `String?` | نوع البيع: قطعة/كرتون/متر/لفة |
| `unitsInLargeUnit` | `double?` | عدد القطع في الكرتون |
| `uniqueId` | `String` | معرف فريد للبند |

### الميزات الخاصة:
*   **Controllers مدمجة:** كل بند يحتوي على `TextEditingController` للربط مع الواجهة.
*   **تنسيق تلقائي للأرقام:** باستخدام `NumberFormat('#,##0.##')`.
*   **Sentinel Pattern:** تقنية متقدمة للسماح بتمرير `null` في `copyWith()`.

---

## 10. محرك المزامنة التفصيلي (`SyncEngine`)

محرك مزامنة متقدم يحتوي على أكثر من **1700 سطر** من الكود.

### مراحل المزامنة:

| المرحلة | الاسم | الوظيفة |
|---------|-------|---------|
| 0 | التحضير المحلي | إنشاء نسخة احتياطية، حساب checksums، جمع العمليات المعلقة |
| 1 | الحصول على القفل | نظام قفل مع heartbeat لمنع التعارض بين الأجهزة |
| 2 | تنزيل الحالة البعيدة | تنزيل manifest وبيانات الأجهزة الأخرى والعمليات الجديدة |
| 3 | التحقق والمصادقة | فحص checksum والتوقيع الرقمي لكل عملية |
| 4 | كشف التعارضات | تحديد العمليات المتعارضة بين الأجهزة |
| 5 | حل التعارضات | تطبيق استراتيجية الحل (LAST_WRITE_WINS افتراضياً) |

### نظام القفل:
```
SyncConfig:
  - lockTimeout: 3 دقائق
  - lockRetryInterval: 10 ثواني
  - maxLockRetries: 5 محاولات
  - heartbeatInterval: 30 ثانية
```

### Verify-After-Write:
*   يتم التحقق بعد كتابة ملف القفل بانتظار عشوائي (200-500ms)
*   إذا وجد أكثر من ملف قفل، الملف الأقدم يفوز
*   يحل مشكلة Race Condition في Google Drive

### استراتيجيات حل التعارض:
| النوع | الوصف |
|-------|-------|
| `LAST_WRITE_WINS` | العملية الأحدث تفوز (افتراضي) |
| `FIRST_WRITE_WINS` | العملية الأقدم تفوز |
| `ASK_USER` | سؤال المستخدم |

### أنواع التعارضات:
*   `DELETE_DELETE` - حذف ضد حذف
*   `DELETE_UPDATE` - حذف ضد تحديث
*   `UPDATE_DELETE` - تحديث ضد حذف
*   `UPDATE_UPDATE` - تحديث ضد تحديث

---

## 11. نموذج عنصر كشف الحساب (`AccountStatementItem`)

نموذج موحد لعرض كل حركة في كشف الحساب.

### الحقول:
| الحقل | النوع | الوصف |
|-------|-------|-------|
| `date` | `DateTime` | تاريخ الحركة |
| `description` | `String` | وصف الحركة |
| `amount` | `double` | المبلغ |
| `type` | `String` | النوع: `invoice` أو `transaction` |
| `invoice` | `Invoice?` | الفاتورة المرتبطة (إن وجدت) |
| `transaction` | `DebtTransaction?` | المعاملة المرتبطة (إن وجدت) |
| `balanceBefore` | `double` | الرصيد قبل الحركة |
| `balanceAfter` | `double` | الرصيد بعد الحركة |

### الدوال المساعدة:
*   `formattedAmount` - تنسيق المبلغ (سالب بأقواس)
*   `formattedDate` - تنسيق التاريخ `YYYY/MM/DD`
*   `formattedBalanceBefore` / `formattedBalanceAfter` - تنسيق الأرصدة

---

## 12. الخلاصة

هذا التطبيق يمثل حلاً متكاملاً لإدارة المبيعات والديون، مع تركيز قوي على:

1.  **الدقة المالية:** استخدام `MoneyCalculator` والتحقق المزدوج.
2.  **سلامة البيانات:** المعاملات الذرية والأقفال المتزامنة.
3.  **المزامنة السحابية:** نظام متقدم مع 6 مراحل، قفل موزع، وحل تعارضات ذكي.
4.  **تجربة المستخدم:** واجهة عربية بديهية مع تقارير شاملة.
5.  **التوثيق:** سندات القبض وكشوفات الحساب PDF.
6.  **حسابات الربح:** عرض تفصيلي للربح لكل بند في الفاتورة.

---

**نهاية التقرير**
